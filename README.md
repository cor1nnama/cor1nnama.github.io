# Corinna Ma — Academic Website Update

This is a drop-in content update for `cor1nnama/cor1nnama.github.io`, which uses the Academic Pages Jekyll template.

## Files to copy

Copy these into the corresponding locations in your GitHub repository:

- `_pages/about.md` — homepage
- `_pages/research.md` — research page
- `_pages/cv.md` — web CV
- `_publications/2020-nice-open-covers.md` — publication entry
- `_data/navigation.yml` — top navigation
- `config_changes.yml` — values to merge into your existing `_config.yml`

## Recommended `_config.yml` changes

Open your repository's existing `_config.yml` and merge the fields from `config_changes.yml`. Do **not** replace the entire Academic Pages configuration with only that file; keep the theme/template settings already present in your repository.

## Publish on GitHub Pages

1. Copy the files above into your repository.
2. Merge the `_config.yml` values.
3. Commit and push to GitHub.
4. GitHub Pages should rebuild the site automatically.

Your public site is:

https://cor1nnama.github.io/

## Content notes

The site is intentionally written for OR / theoretical CS / algorithmic game theory PhD applications. Active research is described as research-in-progress rather than as a publication or completed theorem.
